import "jest"

describe("DriverTaskRecommendationService", () => {
    it('recommends a new non-conflicting time given driver task arguments', () => {
        throw new Error("Not implemented");
    });
    it('recommends a non-conflicting time close to given driver task arguments', () => {
        throw new Error("Not implemented");
    });
    it('will not recommend a time if no non-conflicting time can be scheduled', () => {
        throw new Error("Not implemented");
    });
});
